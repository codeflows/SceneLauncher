/*
 FIXME http://stackoverflow.com/a/19276283
 
 "It turns out that if XCode can't find any C++ files in the project, then it assumes
 that libstd++ is not required. So, you have to manually add a C++ file to the project
 (an empty .mm file would be enough). That is the solution."
*/
#include <stdio.h>
